version: '3'

services:
    artifactory:
        container_name: artifactory
        image: docker.bintray.io/jfrog/artifactory-cpp-ce:latest
        volumes:
            - $JFROG_HOME/artifactory/var/:/var/opt/jfrog/artifactoryname
        ports:
            - 8081:8081
            - 8082:8082