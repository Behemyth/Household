---
driver:
  name: docker
platforms:
  - name: instance
    image: ubuntu:24.04
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  # inventory:
  #   links:
  #     hosts: inventory.yml

verifier:
  name: ansible
