[project]
description = "Home server package"
name = "household"

authors = [
  {name = "Asher Norland", email = "asher.norland@gmail.com"},
]
license = {text = "MIT"}
readme = "README.md"

dynamic = ["version"]

requires-python = ">=3.12" # Pinned to Ubuntu 24 version

dependencies = [
  "ansible>=11.1.0",
  "ansible-vault>=2.1",
  "requests>=2.32.3",
  "pyyaml>=6.0.2",
  "pydantic>=2.10.3",
]

[project.urls]
homepage = "https://github.com/Behemyth/Household"
repository = "https://github.com/Behemyth/Household"

[dependency-groups]
lint = [
  "ruff>=0.8.5",
  "mypy>=1.14",
]
test = [
  "pytest>=8.3.4",
  "pytest-cov>=6.0.0",
  "pytest-mock>=3.14.0",
  "ansible-dev-tools>=4.3.0",
  "molecule-plugins[docker]>=23.6.0",
  "pytest-testinfra[ansible]>=10.1.0",
]

[tool.pytest.ini_options]
log_cli = true
markers = [
  "setup: Tests that should be run prior to any automation, verifying static IP setup",
]
testpaths = [
  "tests",
]

[tool.mypy]
exclude = "__pypackages__"
plugins = ["pydantic.mypy"]
strict = true

[tool.ruff]
line-length = 120
preview = true

[tool.ruff.lint]
ignore = [
  "D206",
  "D300",
  "D415",
  "E111",
  "E114",
  "E117",
]
select = [
  "D", # pydocstyle
  "F", # Pyflakes
  "I", # isort
  "PL", # pylint
  "UP", # pyupgrade
  "E", # pycodestyle
  "B", # flake8-bugbear
  "SIM", # flake8-simplify
  "PT", # flake8-pytest-style
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
docstring-code-format = true
indent-style = "space"
quote-style = "single"

[tool.coverage.report]
skip_empty = true

[tool.pdm.options]
update = ["--update-all"]

[tool.pdm.version]
source = "scm"

[tool.pdm.scripts]
analyze = {cmd = "ruff check household tests"}
ansible-lint = {cmd = "ansible-lint collections"}
dev = {cmd = "molecule converge"}
format = {cmd = "ruff format"}
lint = {composite = ["analyze", "format", "type-check", "ansible-lint"]}
test-molecule.cmd = "molecule test"
test-molecule.working_dir = "collections/ansible_collections/synodic/household/extensions"
test-pytest = {cmd = "pytest --cov=household --verbose tests"}
type-check = {cmd = "mypy ."}

[build-system]
build-backend = "pdm.backend"
requires = ["pdm.backend"]
